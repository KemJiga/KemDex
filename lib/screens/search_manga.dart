import 'package:flutter/material.dart';

class SearchMangaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SearchMangaScreen'),
    );
  }
}
