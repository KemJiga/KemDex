import 'package:flutter/material.dart';

class MangaPageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MangaPageScreen'),
    );
  }
}
